#> dplib:tick
# **Execute every tick the features that need to be refreshed every tick. Put it in your datapack tick/main file before your code or in #minecraft:tick (/minecraft/tags/functions/tick.json) at the top of the list.**
# @public
# @context tick

function dplib:private/tick